#include <stdio.h>
#include <ctype.h>

int main()
{
    printf("Lower case of A is %c\n", tolower('A'));
    printf("Lower case of 9 is %c\n", tolower('9'));
    printf("Lower case of g is %c\n", tolower('g'));

    return 0;
}